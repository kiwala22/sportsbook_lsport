$(document).ready(function() {
  var myChart = echarts.init(document.getElementById('bets_graph'), 'walden');
  var itemStyle = {
      normal: {
      },
      emphasis: {
          barBorderWidth: 1,
          shadowBlur: 10,
          shadowOffsetX: 0,
          shadowOffsetY: 0,
          shadowColor: 'rgba(0,0,0,0.5)'
      }
  };
  var option = {
      title: {
         text: 'Bets Analytics',
         subtext: 'Bet Counts'
      },
      tooltip : {
          trigger: 'axis',
          // axisPointer : {            // 坐标轴指示器，坐标轴触发有效
          //     type : 'shadow'        // 默认为直线，可选为：'line' | 'shadow'
          // }
      },
      legend: {
          data:['Bet Count']
      },
      grid: {
          left: '5%',
          right: '5%',
          bottom: '5%',
          containLabel: true
      },
      xAxis : [
          {
              type : 'category',
              data : gon.dates
          }
      ],
      yAxis : [
          {
              type : 'value'
          }
      ],
      series : [
          {
              name:'Bet Count',
              type:'line',
              itemStyle: {color: 'green'},
              data: gon.bets
          }
      ]
  };

  // use configuration item and data specified to show chart
  myChart.setOption(option);
});

$(document).ready(function(){
   var myChart = echarts.init(document.getElementById('stake_bets_graph'), 'walden');
   var yMax = 500;
   var dataShadow = [];
   option = {
     title: {
        text: 'Bets Analytics',
        subtext: 'Distribution of Bets'
     },
     tooltip : {
           trigger: 'axis',
           axisPointer : {            // 坐标轴指示器，坐标轴触发有效
                 type : 'shadow'        // 默认为直线，可选为：'line' | 'shadow'
           }
     },
     legend: {
         data: ['Stake', 'Amount Won']
     },
     xAxis: {
        data: gon.dates,
        axisTick: {
           show: false
        },
        axisLine: {
           show: false
        },
        z: 10
     },
     yAxis: {
        axisLine: {
           show: false
        },
        axisTick: {
           show: false
        },
        axisLabel: {
           textStyle: {
              color: '#999'
           }
        }
     },
     dataZoom: [
        {
           type: 'inside'
        }
     ],
     series: [

        {
           type: 'bar',
           name: 'Stake',
           data: gon.stake,
        },
        {
           type: 'bar',
           name: 'Amount Won',
           data: gon.amount_won,
        }
     ]
  };
   // use configuration item and data specified to show chart
   myChart.setOption(option);
});
